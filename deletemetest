const words = ['spray', 'limit', ['spray', 'limit', 'elite', 'exuberant', 'destruction', 'present'], 'exuberant', 'destruction', 'present'];

const result = words.filter(word => word.length > 6);

console.log(result);
// expected output: Array ["exuberant", "destruction", "present"]
