<%
  // Create the full string
  var md = ''
  for (var i = 0, l = sections.length; i<l; i++) {
    var section = sections[i];
    if (section.docsText) {
      md += section.docsText
      md += '\n'
    }
    if (section.codeText) {
      md += '```' + lang.name + '\n' + section.codeText + '```\n'
      md += '\n'
    }
  }
%>
<%-md%>